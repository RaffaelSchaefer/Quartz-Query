{
  "name": "quartz_query",
  "version": "0.0.1",
  "description": "A mineral based search engine for my dad",
  "main": "api/build/server.js",
  "scripts": {
    "sass_compile": "npx --no-install sass --no-source-map client/src/:./client/public/",
    "typescript_compile": "tsc --build tsconfig.json",
    "init": "npm run sass_compile && npm run typescript_compile && npm run prettrier",
    "prettrier": "npx prettier --write .",
    "compile": "npm run prettrier && npm run sass_compile && npm run typescript_compile",
    "start_server": "npm run typescript_compile && node api/build/server.js"
  },
  "keywords": [
    "mineral"
  ],
  "author": "Raffael Elias Schäfer",
  "license": "",
  "devDependencies": {
    "csvtojson": "^2.0.10",
    "prettier": "2.4.1",
    "sass": "^1.43.4",
    "typescript": "^4.4.4"
  },
  "dependencies": {
    "axios": "^0.24.0",
    "cors": "^2.8.5",
    "express": "^4.17.1"
  }
}
